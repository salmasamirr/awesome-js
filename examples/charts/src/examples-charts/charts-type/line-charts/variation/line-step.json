{
  "title": "Step Line Schema",
  "allOf": [{ "$ref": "./base-line.schema.json" }],
  "properties": {
    "series": {
      "items": {
        "properties": {
          "step": { "type": "string", "enum": ["start", "middle", "end"] }
        }
      }
    }
  }
}
